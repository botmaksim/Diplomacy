apply plugin: 'com.android.application'

android {
    namespace "com.diplomacy"
    compileSdk 34

    defaultConfig {
        applicationId "com.diplomacy"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation project(':core') // Подключаем тот же общий код
}